const users = {
    name: 'Jessica',
    transactions=[],
    balance=0
}
function createtransactions(transaction){
    users.transaction.push(transaction)
}

if (users.transaction === 'credit'){
    users.balance = user.balance + transaction.value
}else{
    users.balance = user.balance + transaction.value
}




const transactions = {
    type = 'debit',
    value = 200.00
}